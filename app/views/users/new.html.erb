<% provide(:title, 'Sign up') %>
<% provide(:button_text, 'Create my account') %>
<% provide(:url_form, signup_path ) %>

<h1>Sign up</h1>

<div class="row">
  <div class="col-md-6 col-md-offset-3">
    <%#= form_for(@user, url: signup_path) do |f| %>
    <%= render 'form' %>
  </div>
</div>